import { SideBar } from "@/components/components/side-bar"

export const MainContent = () => {
    return (
        <div className="flex justify-center m-4 rounded">
            <SideBar />
        </div>
    )
}